<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= @basis.image %>
</p>
<p>
  <%= @basis.created_at.to_formatted_s(:long_ordinal) %>
</p>

<%= image_tag @basis.image.url %>

<%= link_to 'Edit', edit_basis_path(@basis) %> |
<%= link_to 'Back', bases_path %>
