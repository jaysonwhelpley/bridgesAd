<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= @code.image %>
</p>
<p>
  <%= @code.created_at.to_formatted_s(:long_ordinal) %>
</p>
  <%# image_tag @code.image.url %>

<div class="row">
  <div class="col-sm-8">
    <%= image_tag @code.image.composite.url, class: "col-sm-12"%>
  </div>
</div>


<%= link_to 'Edit', edit_code_path(@code) %> |
<%= link_to 'Back', codes_path %>
